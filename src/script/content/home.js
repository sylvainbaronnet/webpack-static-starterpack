/*
 * home
 */

app.define('content-home', () => {
  /*
  // target phones only
  if (Device.phone) {
  }

  // target tablets only
  if (Device.tablet) {
  }

  // target phones and tablets
  if (Device.mobile) {
  }
*/
});
